# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'ECOnlineDictionary' do
  # Uncomment this line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!
pod ‘RestKit’

  # Pods for ECOnlineDictionary

  target 'ECOnlineDictionaryTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ECOnlineDictionaryUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
